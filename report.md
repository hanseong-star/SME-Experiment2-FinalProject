# DQN 기반 RTT 위치 추정 실험 보고서

## 1. 모티베이션 & 인트로

## 1. 모티베이션 & 인트로

중간발표까지는 UWB와 WiFi를 통해 얻은 RTT 기반 거리값을 이용하여 사용자의 위치를 추정하는 문제를 다루었다. 당시에는 직접 측정한 데이터셋을 바탕으로, 주어진 거리 정보에서 어떻게 하면 더 정확한 위치 측위가 가능한지 분석하고 여러 알고리즘을 적용하여 성능을 검증하였다. 중간발표에서 사용한 주요 알고리즘은 Random Forest였다. Random Forest는 지도학습 기반의 앙상블 모델로, 여러 개의 결정 트리를 조합하여 예측을 수행하기 때문에 비선형적인 데이터 관계를 비교적 안정적으로 학습할 수 있었다. 실제 실험에서도 RTT 거리값과 위치 좌표 사이의 복잡한 관계를 학습하여 의미 있는 위치 추정 결과를 얻을 수 있었다.

하지만 기말 프로젝트에서는 중간발표와 동일하게 지도학습 모델을 사용하는 것보다, 문제를 다른 관점에서 해석해 보고 싶었다. 특히 위치 추정 문제를 단순히 “입력 거리값에서 좌표를 바로 예측하는 회귀 문제”로만 보지 않고, 에이전트가 맵 안에서 이동하면서 정답 위치를 찾아가는 문제로 바꾸어 볼 수 있다고 생각하였다. 이러한 관점은 강화학습의 구조와 잘 맞는다. 강화학습은 에이전트가 현재 상태를 관찰하고, 행동을 선택하며, 그 행동의 결과로 보상을 받아 점차 더 좋은 정책을 학습하는 방식이다. 따라서 위치 추정 문제를 하나의 탐색 게임처럼 정의하면, 지도학습과는 다른 방식으로 좌표를 추정할 수 있을 것이라고 판단하였다.

사용한 데이터셋은 기지국 좌표, 사용자 위치 좌표, 그리고 각 사용자와 기지국 사이의 측정 거리값으로 구성되어 있다. 구체적으로 데이터에는 2 x 18 형태의 기지국 좌표, 2 x 700 형태의 사용자 위치 좌표, 그리고 18 x 700 형태의 측정 거리값이 포함되어 있다. 이 데이터를 강화학습에 적용하기 위해, 사용자가 존재하는 2차원 공간을 하나의 맵으로 보고, 에이전트를 맵의 특정 위치에 배치한 뒤, 에이전트가 위, 아래, 왼쪽, 오른쪽으로 이동하거나 멈추는 행동을 통해 정답 위치를 찾아가도록 문제를 재구성하였다. 즉, 본 프로젝트에서는 “RTT 거리값을 이용해 좌표를 직접 계산하는 문제”를 “거리 정보를 관찰한 에이전트가 맵 안에서 정답 위치로 이동한 뒤 적절한 지점에서 멈추는 문제”로 변환하였다.

이러한 아이디어를 바탕으로 강화학습 실험은 총 5단계로 설계하였다. 첫 번째 단계는 환경 설정이다. 먼저 데이터의 좌표 범위를 확인하여 에이전트가 움직일 수 있는 맵의 크기를 정의하였다. 데이터에서 x 좌표는 대략 -60에서 60 사이에 분포하고, y 좌표는 -30에서 30 사이에 분포한다. 따라서 실험에서는 전체 맵 크기를 120 x 60으로 설정하였다. 두 번째 단계는 상태 정의이다. 에이전트가 의사결정을 하기 위해 관찰하는 상태는 현재 에이전트의 x, y 위치와 18개 기지국으로부터 얻은 거리 데이터로 구성하였다. 즉, 에이전트는 자신의 현재 위치와 RTT 기반 거리 정보를 함께 보고 다음 행동을 선택한다.

세 번째 단계는 행동 정의이다. 본 문제에서는 연속적인 이동 제어가 아니라 격자 형태의 단순한 이동 문제로 접근하였다. 따라서 행동은 위로 이동, 아래로 이동, 왼쪽으로 이동, 오른쪽으로 이동, 멈춤의 총 5개로 정의하였다. 네 번째 단계는 보상 설계이다. 에이전트가 정답 위치에 가까워지는 방향으로 이동하면 양의 보상을 주고, 정답 위치에서 멀어지는 방향으로 이동하면 패널티를 주었다. 또한 정답 위치 근처에서 멈추면 큰 보상을 주고, 정답 위치와 먼 곳에서 멈추면 큰 패널티를 주도록 설계하였다. 이 보상 구조를 통해 에이전트가 단순히 오래 움직이는 것이 아니라, 정답 위치에 가까워진 뒤 적절한 시점에 멈추는 정책을 학습하도록 유도하였다.

마지막 다섯 번째 단계는 강화학습 알고리즘 선택이다. 본 실험에서는 DQN을 사용하였다. DQN은 Deep Q-Network의 약자로, Q-Learning에 딥러닝 신경망을 결합한 강화학습 알고리즘이다. 일반적인 Q-Learning은 상태와 행동의 조합이 많아지면 Q-table을 직접 저장하기 어렵지만, DQN은 신경망을 사용하여 각 행동의 Q값을 근사할 수 있다. 본 프로젝트의 행동 공간은 위, 아래, 왼쪽, 오른쪽, 멈춤처럼 이산적인 행동으로 구성되어 있기 때문에 DQN을 적용하기에 적합하다고 판단하였다. DQN 모델은 현재 상태를 입력으로 받아 5개 행동에 대한 Q값을 출력하고, 그중 가장 높은 Q값을 가지는 행동을 선택한다. 이를 반복하면서 에이전트는 주어진 RTT 거리 정보에 대해 어느 방향으로 이동해야 정답 위치에 가까워지는지 학습하게 된다.

결과적으로 본 프로젝트의 핵심 아이디어는 위치 추정 문제를 강화학습 기반의 순차적 의사결정 문제로 변환한 것이다. 중간발표에서 사용한 Random Forest는 입력 거리값으로부터 위치 좌표를 직접 예측하는 지도학습 방식이었다면, 이번 실험의 DQN은 에이전트가 현재 위치에서 시작하여 행동을 반복하며 정답 위치를 찾아가는 방식이다. 따라서 두 접근은 같은 RTT 기반 위치 추정 문제를 다루지만, 문제를 바라보는 관점과 학습 방식이 다르다. 

비교 기준으로는 물리 기반 RTT 보정 + Weighted Robust Least Squares + Residual Correction 실험을 사용하였다. 이 비교 실험은 기지국별 bias와 신뢰도를 명시적으로 계산하고, Ridge Regression으로 잔여 오차를 보정하는 방식이다. 반면 본 보고서의 중심은 DQN 기반 강화학습 모델이며, 물리 기반 실험은 강화학습 결과가 어느 정도 수준인지 판단하기 위한 비교용 baseline으로만 사용하였다.

데이터 셋으로 학습한 모델의 성능을 평가하기 위해 700개의 data set중 500개는 train dataset으로 200개는 validation dataset으로 구분하여 실험을 진행하였다.

전체 실험의 핵심 질문은 다음과 같다.

| 질문 | 의미 |
|---|---|
| RTT 위치 추정 문제를 강화학습 환경으로 바꿀 수 있는가? | 위치 추정을 순차적 의사결정 문제로 모델링할 수 있는지 확인한다. |
| DQN이 이동과 정지 행동을 통해 위치를 찾을 수 있는가? | 거리값 18개와 현재 위치를 상태로 사용했을 때 정책 학습이 가능한지 확인한다. |
| 보상 설계가 성능에 어떤 영향을 주는가? | STOP 보상, 이동 보상, 성공 반경 변화에 따른 성능 변화를 분석한다. |
| 물리 기반 baseline과 비교했을 때 어떤 한계가 있는가? | 강화학습 접근의 장점과 단점을 공정하게 해석한다. |

## 2. 알고리즘 설명

본 프로젝트의 핵심 알고리즘은 DQN(Deep Q-Network)을 이용한 강화학습 기반 위치 추정 알고리즘이다. 일반적인 위치 추정 알고리즘은 측정 거리값을 입력으로 받아 사용자 좌표를 직접 계산하거나 예측한다. 그러나 본 실험에서는 위치 추정 문제를 에이전트가 2차원 맵 안에서 이동하면서 정답 위치를 찾아가는 순차적 의사결정 문제로 변환하였다.

즉, 하나의 사용자 샘플에 대해 정답 위치가 존재하고, 에이전트는 맵의 특정 위치에서 시작한다. 에이전트는 현재 위치와 18개 기지국으로부터 얻은 거리 정보를 상태로 관찰하고, 위, 아래, 왼쪽, 오른쪽, 멈춤 중 하나의 행동을 선택한다. 이동 행동을 선택하면 에이전트의 위치가 한 칸 이동하고, 멈춤 행동을 선택하면 현재 위치를 최종 추정 위치로 출력한다. 학습 과정에서는 정답 위치를 알고 있으므로, 에이전트가 정답 위치에 가까워지는 행동을 하면 보상을 주고, 멀어지는 행동을 하면 패널티를 준다. 이를 반복하여 에이전트가 거리 정보에 따라 어느 방향으로 이동하고 언제 멈춰야 하는지를 학습하도록 한다.

### 2.1 문제 정의

데이터셋은 사용자 위치, 기지국 위치, 측정 거리값으로 구성된다. 사용자 샘플의 인덱스를 $u$라고 하면, 해당 샘플의 정답 위치는 다음과 같이 정의한다.

$$
p_u = (x_u, y_u)
$$

여기서 $x_u$와 $y_u$는 사용자 $u$의 실제 좌표이다.

18개 기지국으로부터 얻은 측정 거리값은 다음과 같이 정의한다.

$$
d_u = [d_{1,u}, d_{2,u}, \cdots, d_{18,u}]
$$

여기서 $d_{i,u}$는 사용자 $u$에 대해 $i$번째 기지국으로부터 얻은 RTT 기반 측정 거리값이다.

강화학습에서는 하나의 사용자 샘플 $u$를 하나의 episode에서 찾아야 할 목표로 사용한다. episode가 시작될 때 사용자 샘플 하나를 선택하고, 해당 샘플의 정답 위치 $p_u$와 거리 정보 $d_u$를 고정한다. 이후 에이전트는 맵 안에서 이동하면서 최종적으로 $p_u$에 가까운 위치에서 멈추는 것을 목표로 한다.

### 2.2 환경 정의

에이전트가 움직이는 맵은 데이터의 좌표 범위를 기준으로 설정하였다. 사용자 위치 데이터의 범위를 확인하면 $x$ 좌표는 약 $-60$부터 $60$까지, $y$ 좌표는 약 $-30$부터 $30$까지 분포한다. 따라서 실험에서 사용하는 맵의 범위는 다음과 같이 정의한다.

$$
X_{\min} = -60,\quad X_{\max} = 60
$$

$$
Y_{\min} = -30,\quad Y_{\max} = 30
$$

즉, 전체 맵의 크기는 $120 \times 60$이다. 에이전트의 현재 위치를 다음과 같이 정의한다.

$$
\hat{p}_t = (\hat{x}_t, \hat{y}_t)
$$

여기서 $t$는 현재 step을 의미한다. 에이전트는 매 step마다 하나의 행동을 선택하고, 선택한 행동에 따라 위치를 갱신한다.

이동 단위는 다음과 같이 설정한다.

$$
\Delta = 1.0
$$

따라서 한 번 이동할 때마다 에이전트의 좌표는 1만큼 변한다. 에이전트가 맵 밖으로 나가지 않도록 위치는 항상 다음 범위로 제한한다.

$$
\hat{x}*t \in [X*{\min}, X_{\max}]
$$

$$
\hat{y}*t \in [Y*{\min}, Y_{\max}]
$$

만약 이동 후 좌표가 맵의 범위를 벗어나면, 해당 위치는 맵의 경계 안으로 다시 제한한다. 즉, $x$ 좌표와 $y$ 좌표는 다음과 같이 clipping한다.

$$
\hat{x}*t = \min(\max(\hat{x}*t, X*{\min}), X*{\max})
$$

$$
\hat{y}*t = \min(\max(\hat{y}*t, Y*{\min}), Y*{\max})
$$

한 episode에서 에이전트가 이동할 수 있는 최대 step 수는 다음과 같이 제한한다.

$$
T_{\max} = 200
$$

에이전트가 $T_{\max}$ step 안에 멈춤 행동을 선택하지 않으면 episode를 종료한다.

### 2.3 상태 정의

DQN의 입력은 현재 상태 $s_t$이다. 본 실험에서 상태는 에이전트의 현재 위치와 18개 기지국의 측정 거리값으로 구성된다.

에이전트의 현재 위치는 $(\hat{x}_t, \hat{y}_t)$이고, 선택된 사용자 샘플 $u$의 거리 정보는 $d_u$이다. 따라서 상태는 다음과 같이 정의된다.

$$
s_t = [\hat{x}*t, \hat{y}*t, d*{1,u}, d*{2,u}, \cdots, d_{18,u}]
$$

하지만 좌표와 거리값의 범위가 서로 다르기 때문에, 신경망에 입력하기 전에 정규화를 수행한다. 먼저 현재 위치는 맵의 범위를 기준으로 0과 1 사이의 값으로 변환한다.

$$
\hat{x}*{t,n} = \frac{\hat{x}*t - X*{\min}}{X*{\max} - X_{\min}}
$$

$$
\hat{y}*{t,n} = \frac{\hat{y}*t - Y*{\min}}{Y*{\max} - Y_{\min}}
$$

거리값은 일정한 스케일 값으로 나누어 정규화한다. 본 실험에서는 거리값을 320으로 나누었다.

$$
d_{i,u,n} = \frac{d_{i,u}}{320}
$$

따라서 DQN에 실제로 입력되는 상태 벡터는 다음과 같다.

$$
s_t =
[
\hat{x}*{t,n},
\hat{y}*{t,n},
d_{1,u,n},
d_{2,u,n},
\cdots,
d_{18,u,n}
]
$$

상태 벡터의 차원은 현재 위치 2개와 거리값 18개를 합쳐 총 20차원이다.

$$
\dim(s_t) = 2 + 18 = 20
$$

이 상태 정의의 의미는 간단하다. 에이전트는 자신의 현재 위치와 해당 사용자 샘플의 거리 정보를 함께 보고 다음 행동을 결정한다. 같은 위치에 있더라도 거리 정보가 다르면 정답 위치가 다르기 때문에, 거리 정보는 정책을 결정하는 핵심 입력으로 사용된다.

### 2.4 행동 정의

본 실험에서는 연속적인 좌표 이동이 아니라 이산적인 행동 공간을 사용한다. 에이전트가 선택할 수 있는 행동은 총 5개이다.

$$
A = {0, 1, 2, 3, 4}
$$

각 행동의 의미는 다음과 같다.

| 행동 번호 | 행동 의미    | 좌표 변화                                |
| ----: | -------- | ------------------------------------ |
|     0 | 위로 이동    | $\hat{y}_{t+1} = \hat{y}_t + \Delta$ |
|     1 | 아래로 이동   | $\hat{y}_{t+1} = \hat{y}_t - \Delta$ |
|     2 | 왼쪽으로 이동  | $\hat{x}_{t+1} = \hat{x}_t - \Delta$ |
|     3 | 오른쪽으로 이동 | $\hat{x}_{t+1} = \hat{x}_t + \Delta$ |
|     4 | 멈춤       | 현재 위치를 최종 추정 위치로 출력                  |

이동 행동인 0, 1, 2, 3을 선택하면 에이전트의 위치가 갱신된다. 반면 행동 4를 선택하면 episode가 종료되고, 현재 위치가 해당 사용자 샘플의 최종 추정 위치가 된다.

즉, 멈춤 행동을 선택한 시점의 위치가 다음과 같이 최종 예측값이 된다.

$$
\hat{p}_u = (\hat{x}_t, \hat{y}_t)
$$

### 2.5 위치 오차 정의

보상을 계산하기 위해 에이전트의 현재 위치와 정답 위치 사이의 유클리드 거리를 사용한다. 현재 step $t$에서의 위치 오차는 다음과 같이 정의한다.

$$
e_t = \sqrt{(\hat{x}_t - x_u)^2 + (\hat{y}_t - y_u)^2}
$$

여기서 $e_t$가 작을수록 에이전트가 정답 위치에 가까운 상태라는 의미이다. 이동 전 오차를 $e_{t-1}$, 이동 후 오차를 $e_t$라고 하면, 두 오차의 차이는 다음과 같이 정의할 수 있다.

$$
\Delta e_t = e_{t-1} - e_t
$$

만약 $\Delta e_t > 0$이면 에이전트가 정답 위치에 가까워진 것이고, $\Delta e_t < 0$이면 정답 위치에서 멀어진 것이다. 이 값을 이동 행동의 보상으로 사용한다.

### 2.6 보상 설계

보상 함수는 강화학습 성능에 가장 큰 영향을 주는 요소이다. 본 실험에서는 이동 행동에 대한 보상과 멈춤 행동에 대한 보상을 나누어 설계하였다.

먼저 이동 행동에 대한 보상은 에이전트가 정답 위치에 가까워졌는지 여부를 기준으로 계산한다. 이동 전 오차를 $e_{t-1}$, 이동 후 오차를 $e_t$라고 할 때, 이동 보상은 다음과 같이 정의한다.

$$
r_t = \alpha (e_{t-1} - e_t) - \beta
$$

여기서 $\alpha$는 거리 감소 보상의 크기를 조절하는 계수이고, $\beta$는 매 step마다 부여되는 시간 패널티이다. 본 실험에서는 다음 값을 사용하였다.

$$
\alpha = 1.0
$$

$$
\beta = 0.01
$$

따라서 에이전트가 정답 위치에 가까워지면 $e_{t-1} - e_t$가 양수가 되어 보상을 받는다. 반대로 정답 위치에서 멀어지면 이 값이 음수가 되어 패널티를 받는다. 또한 매 step마다 $0.01$의 시간 패널티를 주어, 에이전트가 불필요하게 오래 움직이지 않도록 하였다.

다음으로 멈춤 행동에 대한 보상을 정의한다. 멈춤 행동은 최종 위치 추정 결과를 결정하는 행동이므로, 현재 위치가 정답 위치와 얼마나 가까운지에 따라 보상을 크게 다르게 주었다. 멈춤 시점의 오차를 $e_t$라고 하면, 멈춤 보상은 다음과 같이 정의한다.

$$
r_t =
\begin{cases}
R_{\text{success}}, & e_t \leq r_{\text{success}} \
R_{\text{mid}}, & r_{\text{success}} < e_t \leq r_{\text{mid}} \
R_{\text{fail}}, & e_t > r_{\text{mid}}
\end{cases}
$$

본 실험의 기본 설정은 다음과 같다.

$$
r_{\text{success}} = 2.0
$$

$$
r_{\text{mid}} = 5.0
$$

$$
R_{\text{success}} = 20.0
$$

$$
R_{\text{mid}} = 5.0
$$

$$
R_{\text{fail}} = -20.0
$$

즉, 에이전트가 정답 위치로부터 2m 이내에서 멈추면 큰 양의 보상을 받는다. 2m보다 멀지만 5m 이내에서 멈추면 중간 정도의 양의 보상을 받는다. 5m보다 먼 위치에서 멈추면 잘못된 위치에서 멈춘 것으로 판단하여 큰 음의 보상을 받는다.

맵 밖으로 이동하려는 행동에 대해서도 패널티를 부여한다. 이동 결과가 맵 범위를 벗어나면 다음 패널티를 추가한다.

$$
R_{\text{out}} = -5.0
$$

이 경우 에이전트의 위치는 맵 밖으로 나가지 않도록 이전 위치로 되돌리거나 경계 안으로 제한한다. 이를 통해 에이전트가 유효하지 않은 방향으로 계속 이동하는 정책을 학습하지 않도록 한다.

### 2.7 DQN 모델 구조

DQN은 현재 상태 $s_t$를 입력으로 받아 각 행동에 대한 Q값을 출력하는 신경망이다. Q값은 특정 상태에서 특정 행동을 선택했을 때 앞으로 받을 것으로 기대되는 누적 보상을 의미한다.

DQN은 다음 함수를 근사한다.

$$
Q(s_t, a_t; \theta)
$$

여기서 $\theta$는 신경망의 파라미터이다. 본 실험의 DQN은 20차원 상태 벡터를 입력으로 받고, 5개 행동에 대한 Q값을 출력한다.

입력 차원은 다음과 같다.

$$
\text{input dimension} = 20
$$

출력 차원은 행동 개수와 같으므로 다음과 같다.

$$
\text{output dimension} = 5
$$

신경망 구조는 다음과 같이 구성한다.

$$
20 \rightarrow 128 \rightarrow 128 \rightarrow 5
$$

첫 번째 은닉층과 두 번째 은닉층에는 ReLU 활성화 함수를 사용한다. 따라서 모델은 다음과 같이 표현할 수 있다.

$$
h_1 = \text{ReLU}(W_1 s_t + b_1)
$$

$$
h_2 = \text{ReLU}(W_2 h_1 + b_2)
$$

$$
Q(s_t, \cdot) = W_3 h_2 + b_3
$$

최종 출력은 5차원 벡터이다.

$$
Q(s_t, \cdot) =
[
Q(s_t, 0),
Q(s_t, 1),
Q(s_t, 2),
Q(s_t, 3),
Q(s_t, 4)
]
$$

추론 시에는 이 중 가장 큰 Q값을 가지는 행동을 선택한다.

$$
a_t = \arg\max_a Q(s_t, a)
$$

### 2.8 경험 저장과 Replay Buffer

DQN은 에이전트가 환경과 상호작용하면서 얻은 경험을 replay buffer에 저장한다. 하나의 경험은 다음과 같은 형태로 저장된다.

$$
(s_t, a_t, r_t, s_{t+1}, done)
$$

여기서 $s_t$는 현재 상태, $a_t$는 선택한 행동, $r_t$는 보상, $s_{t+1}$은 다음 상태, $done$은 episode 종료 여부이다.

Replay buffer를 사용하는 이유는 최근 경험만으로 학습하면 데이터 간 상관성이 커져 학습이 불안정해질 수 있기 때문이다. 따라서 저장된 경험 중 일부를 무작위로 샘플링하여 mini-batch를 만들고 학습에 사용한다.

본 실험에서는 replay buffer의 최대 크기를 다음과 같이 설정하였다.

$$
M = 50000
$$

mini-batch 크기는 다음과 같이 설정하였다.

$$
B = 64
$$

즉, buffer에 충분한 경험이 쌓이면 매 학습 step마다 64개의 경험을 무작위로 뽑아 DQN을 업데이트한다.

### 2.9 행동 선택 방법

학습 과정에서는 탐험과 활용의 균형이 필요하다. 처음부터 Q값이 가장 큰 행동만 선택하면 다양한 행동을 충분히 경험하지 못할 수 있다. 따라서 $\epsilon$-greedy 방법을 사용한다.

$\epsilon$-greedy 정책은 다음과 같다.

$$
a_t =
\begin{cases}
\text{random action}, & \text{with probability } \epsilon \
\arg\max_a Q(s_t, a; \theta), & \text{with probability } 1-\epsilon
\end{cases}
$$

즉, 확률 $\epsilon$으로는 무작위 행동을 선택하고, 확률 $1-\epsilon$으로는 현재 DQN이 가장 좋다고 판단한 행동을 선택한다.

초기에는 탐험을 많이 하기 위해 $\epsilon$을 크게 설정한다.

$$
\epsilon_{\text{start}} = 1.0
$$

학습이 진행될수록 점점 $\epsilon$을 줄인다.

$$
\epsilon_{\text{end}} = 0.05
$$

매 episode가 끝날 때마다 다음과 같이 $\epsilon$을 감소시킨다.

$$
\epsilon \leftarrow \max(\epsilon_{\text{end}}, \epsilon \cdot \epsilon_{\text{decay}})
$$

본 실험에서는 다음 decay 값을 사용하였다.

$$
\epsilon_{\text{decay}} = 0.995
$$

따라서 학습 초반에는 무작위 이동을 많이 하며 다양한 경로를 탐색하고, 학습 후반에는 DQN이 예측한 Q값을 기반으로 행동을 선택한다.

### 2.10 DQN 학습 목표

DQN은 Bellman equation을 기반으로 학습한다. 하나의 경험 $(s_t, a_t, r_t, s_{t+1}, done)$이 있을 때, 현재 Q값은 다음과 같다.

$$
Q(s_t, a_t; \theta)
$$

학습의 목표값, 즉 target Q값은 다음과 같이 정의한다.

$$
y_t =
\begin{cases}
r_t, & done = 1 \
r_t + \gamma \max_{a'} Q_{\text{target}}(s_{t+1}, a'; \theta^-), & done = 0
\end{cases}
$$

여기서 $\gamma$는 할인율이고, $\theta^-$는 target network의 파라미터이다. 본 실험에서는 할인율을 다음과 같이 설정하였다.

$$
\gamma = 0.98
$$

할인율이 1에 가까우면 현재 보상뿐 아니라 미래 보상도 중요하게 고려한다. 위치 추정 문제에서는 당장 한 step의 이동보다 최종적으로 정답 위치에 도달하는 것이 중요하므로, 비교적 큰 할인율을 사용하였다.

DQN의 손실 함수는 현재 Q값과 target Q값의 차이로 정의한다.

$$
L(\theta) =
\frac{1}{B}
\sum_{i=1}^{B}
\left(
Q(s_i, a_i; \theta) - y_i
\right)^2
$$

여기서 $B$는 mini-batch 크기이다. 이 손실을 줄이는 방향으로 신경망 파라미터 $\theta$를 업데이트한다. 최적화 알고리즘으로는 Adam optimizer를 사용하고, learning rate는 다음과 같이 설정하였다.

$$
\eta = 0.001
$$

### 2.11 Target Network

DQN에서는 학습 안정성을 높이기 위해 policy network와 target network를 분리한다. Policy network는 매 학습 step마다 업데이트되는 네트워크이고, target network는 target Q값을 계산할 때 사용하는 네트워크이다.

Policy network의 파라미터를 $\theta$, target network의 파라미터를 $\theta^-$라고 하면, 학습 중에는 $\theta$만 계속 업데이트한다. 그리고 일정 step마다 target network를 policy network와 동일하게 복사한다.

$$
\theta^- \leftarrow \theta
$$

본 실험에서는 다음 간격마다 target network를 업데이트한다.

$$
C = 100
$$

즉, 100 step마다 policy network의 가중치를 target network에 복사한다. 이를 통해 target Q값이 너무 빠르게 변하는 것을 막고, 학습을 더 안정적으로 만든다.

### 2.12 학습 절차

전체 학습 과정은 다음 순서로 진행된다.

먼저 train 데이터에서 사용자 샘플 하나를 무작위로 선택한다. 선택된 샘플의 정답 위치 $p_u$와 거리 정보 $d_u$를 현재 episode의 목표로 설정한다. 그다음 에이전트의 초기 위치를 설정한다. 초기 위치는 맵의 중앙으로 둘 수도 있고, 무작위 위치로 둘 수도 있다. 본 실험에서는 다양한 시작 위치를 경험하도록 random 시작 위치를 사용하였다.

초기 상태 $s_0$를 만든 뒤, 에이전트는 최대 $T_{\max}$ step 동안 행동을 반복한다. 각 step에서 에이전트는 $\epsilon$-greedy 방식으로 행동 $a_t$를 선택한다. 선택한 행동이 이동 행동이면 좌표를 갱신하고 이동 보상을 계산한다. 선택한 행동이 멈춤이면 현재 위치와 정답 위치의 오차를 기준으로 멈춤 보상을 계산하고 episode를 종료한다.

각 step에서 생성된 경험 $(s_t, a_t, r_t, s_{t+1}, done)$은 replay buffer에 저장된다. Replay buffer에 mini-batch 크기 이상의 경험이 쌓이면, 무작위로 $B$개의 경험을 샘플링하여 DQN을 학습한다. 이때 target Q값을 계산하고, 현재 Q값과 target Q값의 차이를 줄이도록 policy network를 업데이트한다.

이 과정을 정해진 episode 수만큼 반복한다. 본 실험의 기본 episode 수는 다음과 같다.

$$
N_{\text{episode}} = 3000
$$

학습이 완료되면 policy network의 가중치를 저장하고, validation 또는 test 단계에서 해당 모델을 불러와 위치 추정을 수행한다.

### 2.13 추론 과정

학습이 끝난 뒤에는 더 이상 정답 위치를 사용하여 보상을 계산하지 않는다. Validation 또는 test 단계에서는 측정 거리값 $d_u$만 주어진 상태에서 학습된 DQN을 이용해 위치를 추정한다.

추론 과정은 다음과 같다. 먼저 에이전트의 초기 위치를 설정한다. 본 실험에서는 기본적으로 맵의 중앙을 시작 위치로 사용한다.

$$
\hat{x}*0 = \frac{X*{\min} + X_{\max}}{2}
$$

$$
\hat{y}*0 = \frac{Y*{\min} + Y_{\max}}{2}
$$

현재 맵 설정에서는 다음과 같다.

$$
\hat{x}_0 = 0,\quad \hat{y}_0 = 0
$$

그다음 현재 위치와 입력 거리값 $d_u$를 이용해 상태 $s_t$를 만든다. 이 상태를 학습된 DQN에 넣으면 5개 행동에 대한 Q값이 출력된다.

$$
Q(s_t, \cdot) =
[
Q(s_t, 0),
Q(s_t, 1),
Q(s_t, 2),
Q(s_t, 3),
Q(s_t, 4)
]
$$

추론 단계에서는 탐험이 필요 없으므로 무작위 행동을 선택하지 않고, 항상 Q값이 가장 큰 행동을 선택한다.

$$
a_t = \arg\max_a Q(s_t, a)
$$

선택한 행동이 이동 행동이면 위치를 갱신하고 다음 step으로 넘어간다. 선택한 행동이 멈춤이면 현재 위치를 최종 예측 위치로 출력한다.

$$
\hat{p}_u = (\hat{x}_t, \hat{y}_t)
$$

만약 최대 step 수인 $T_{\max}$에 도달할 때까지 멈춤 행동을 선택하지 않으면, 마지막 step에서의 위치를 최종 예측 위치로 사용한다.

### 2.14 최종 위치 오차 계산

모든 validation 샘플에 대해 예측 위치를 구한 뒤, 각 샘플의 위치 오차를 계산한다. 사용자 $u$에 대한 예측 위치를 $\hat{p}_u = (\hat{x}_u, \hat{y}_u)$, 실제 위치를 $p_u = (x_u, y_u)$라고 하면, 위치 오차는 다음과 같다.

$$
e_u =
\sqrt{
(\hat{x}_u - x_u)^2
+
(\hat{y}_u - y_u)^2
}
$$

전체 샘플 수를 $N$이라고 할 때, 평균 오차는 다음과 같다.

$$
\mathrm{Mean\ Error} = \frac{1}{N}\sum_{u=1}^{N} e_u
$$

RMSE는 다음과 같이 계산한다.

$$
\mathrm{RMSE} = \sqrt{\frac{1}{N}\sum_{u=1}^{N} e_u^2}
$$

또한 중앙값 오차, 90% 오차, 최대 오차를 함께 계산한다. 평균 오차만 사용하면 일부 샘플에서 크게 실패하는 경우를 확인하기 어렵기 때문에, 본 실험에서는 다음 지표를 모두 사용한다.

| 지표           | 의미                          |
| ------------ | --------------------------- |
| Mean Error   | 전체 위치 오차의 평균                |
| Median Error | 전체 위치 오차의 중앙값               |
| RMSE         | 큰 오차에 민감한 평균 제곱근 오차         |
| 90% Error    | 전체 샘플 중 90%가 이 값 이하의 오차를 가짐 |
| Max Error    | 가장 크게 실패한 샘플의 오차            |

### 2.15 알고리즘의 핵심 동작 요약

본 알고리즘은 입력 거리값으로부터 좌표를 한 번에 직접 예측하지 않는다. 대신 에이전트를 2차원 맵 위에 배치하고, 현재 위치와 거리 정보를 바탕으로 한 step씩 이동시킨다. 학습 과정에서는 정답 위치를 알고 있으므로, 정답에 가까워지는 이동에는 보상을 주고, 멀어지는 이동에는 패널티를 준다. 또한 정답 근처에서 멈추면 큰 보상을 주고, 잘못된 위치에서 멈추면 큰 패널티를 준다.

DQN은 이러한 경험을 반복적으로 학습하면서 각 상태에서 어떤 행동이 장기적으로 높은 보상을 주는지 근사한다. 최종적으로 학습된 DQN은 주어진 거리 정보와 현재 위치를 입력받아 위, 아래, 왼쪽, 오른쪽, 멈춤 중 가장 적절한 행동을 선택한다. 이 과정을 멈춤 행동이 나올 때까지 반복하고, 멈춘 위치를 최종 사용자 위치 추정값으로 사용한다.

따라서 본 알고리즘의 전체 구조는 다음과 같이 정리할 수 있다.

$$
\text{RTT distance data}
\rightarrow
\text{state construction}
\rightarrow
\text{DQN action selection}
\rightarrow
\text{agent movement}
\rightarrow
\text{STOP action}
\rightarrow
\text{estimated position}
$$

이 방식은 기존의 지도학습 기반 위치 추정 방식과 다르게, 위치 추정을 순차적 탐색 문제로 변환했다는 점에서 차이가 있다. Random Forest와 같은 지도학습 모델은 거리값을 입력으로 받아 좌표를 직접 출력하지만, DQN 기반 방법은 거리 정보를 조건으로 사용하여 에이전트가 맵 위에서 행동을 반복하며 위치를 찾아간다. 즉, 본 실험의 DQN 알고리즘은 거리 기반 위치 추정 문제를 강화학습의 상태, 행동, 보상 구조로 재정의한 방법이다.


## 3. Agent AI 활용 방안

본 프로젝트에서는 강화학습 기반 RTT 위치 추정 알고리즘을 설계하고 구현하는 과정에서 Agent AI를 보조 도구로 활용하였다. 사용한 Agent AI는 주로 ChatGPT이며, 강화학습 개념 이해, DQN 알고리즘 구조 정리, 코드 구성 방식 검토, 실험 결과 해석, 보고서 문장 정리에 활용하였다. 다만 강화학습을 프로젝트의 메인 방법으로 선택한 것, 위치 추정 문제를 에이전트가 맵 위에서 이동하며 정답 위치를 찾는 문제로 재구성한 것, 상태·행동·보상 구조를 설계한 것, 실험 방향을 정한 것은 본인이 직접 수행하였다.

처음에는 중간발표에서 사용한 Random Forest 기반 위치 추정 방식과 다른 접근을 시도하고 싶었다. 중간발표에서는 UWB와 WiFi 기반 RTT 거리값을 입력으로 사용하여 지도학습 모델이 위치 좌표를 직접 예측하도록 하였다. 그러나 기말 프로젝트에서는 지도학습이 아닌 다른 방식으로 문제를 풀어보고 싶었고, 본인이 관심을 가지고 있던 강화학습을 적용하기로 결정하였다. 이 과정에서 ChatGPT는 강화학습이 데이터 수가 적고 정답 좌표가 존재하는 위치 추정 문제에서는 반드시 유리한 방법은 아닐 수 있다고 설명하였다. 하지만 본인은 위치 추정 문제를 단순 회귀 문제가 아니라 에이전트가 맵 안에서 이동하며 정답 위치를 찾아가는 문제로 바꾸면 강화학습 실험이 가능하다고 판단하였고, 최종적으로 강화학습을 메인 알고리즘으로 선택하였다.

강화학습 문제 설계 역시 본인이 주도하였다. 본인은 데이터셋의 좌표 범위를 확인하여 맵을 `x = -60 ~ 60`, `y = -30 ~ 30`으로 설정하였다. 또한 에이전트의 현재 위치와 18개 기지국으로부터 얻은 RTT 거리값을 상태로 사용하고, 행동은 위, 아래, 왼쪽, 오른쪽, 멈춤의 5개로 정의하였다. 보상은 정답 위치에 가까워지는 이동에는 양의 보상을 주고, 멀어지는 이동에는 패널티를 주며, 정답 근처에서 멈추면 큰 보상을 주고, 정답과 먼 곳에서 멈추면 큰 패널티를 주는 방식으로 설계하였다. ChatGPT는 이러한 설계가 강화학습의 환경, 상태, 행동, 보상 구조로 어떻게 표현될 수 있는지 정리하고, DQN과 연결해 설명하는 보조 역할을 하였다.

알고리즘 구현 단계에서는 ChatGPT를 활용하여 DQN의 기본 구성 요소를 이해하고 코드 구조를 검토하였다. DQN을 구현하기 위해서는 replay buffer, policy network, target network, epsilon-greedy 탐험 전략, Q-value 업데이트 과정이 필요하다. ChatGPT는 이러한 구성 요소들이 어떤 역할을 하는지 설명하고, 학습 흐름을 정리하는 데 도움을 주었다. 예를 들어 하나의 episode에서 학습 샘플 하나를 선택하고, 에이전트의 초기 위치를 설정한 뒤, 현재 상태를 만들고 행동을 선택하며 보상을 계산하는 흐름을 이해하는 데 활용하였다. 또한 replay buffer에 `(state, action, reward, next_state, done)` 형태의 경험을 저장하고, mini-batch를 이용하여 Bellman target을 계산하는 구조를 검토하는 데 참고하였다.

코드 구성 측면에서는 `train.py`와 `main.py`를 분리하여 작성하였다. `train.py`에서는 `train_500.mat` 데이터를 이용해 DQN 모델을 학습하고, 학습된 모델을 `dqn_model.pt`로 저장하도록 구성하였다. `main.py`에서는 저장된 모델을 불러와 `validation_200.mat` 데이터에 대해 위치를 추정하고, 예측 좌표 `p_hat`을 계산하도록 구성하였다. 이 과정에서 ChatGPT는 코드 구조와 함수 역할을 점검하는 보조 역할을 하였고, 실제 파일 경로 설정, 코드 실행, 오류 수정, 모델 학습, validation 결과 확인은 본인이 직접 수행하였다.

실험 설계 역시 본인이 직접 수행하였다. 본인은 강화학습의 성능을 확인하기 위해 시작 위치를 center로 둘지 random으로 둘지, step size와 max step을 어떻게 설정할지, learning rate와 epsilon decay를 어떻게 조정할지, Double DQN을 적용할지, 기지국 위치 정보를 state에 추가할지 등을 직접 정하고 실험하였다. 또한 STOP 성공 반경, STOP 실패 패널티, 이동 시간 패널티, 거리 감소 보상, Gaussian 보상, Tanh 보상 등 다양한 보상 구조를 직접 설계하고 변경하며 성능을 비교하였다. 각 실험 결과는 mean error, median error, RMSE, 90% error, max error를 기준으로 평가하였다.

ChatGPT는 실험 결과가 왜 좋아지거나 나빠졌는지 해석하는 데 사용하였다. 예를 들어 Gaussian 형태의 부드러운 STOP 보상이나 Tanh 기반 보상은 이론적으로는 부드러운 학습 신호를 줄 수 있지만, 실제 실험에서는 baseline보다 오차가 증가하였다. 이에 대해 ChatGPT는 STOP 보상이 지나치게 부드러워지면 에이전트가 언제 멈춰야 하는지 명확하게 학습하지 못할 수 있다는 해석을 제시하였다. 또한 멀리서 멈추는 행동에 대한 패널티가 너무 강하면 에이전트가 STOP 행동 자체를 회피할 수 있다는 점도 분석에 참고하였다. 그러나 이러한 해석은 실험 결과를 이해하기 위한 참고로 사용하였고, 어떤 설정을 유지하거나 제거할지는 본인이 validation 결과를 비교하여 판단하였다.

비교 실험 정리 과정에서도 ChatGPT를 활용하였다. 본 프로젝트의 메인 알고리즘은 강화학습 기반 DQN이지만, 실험 1에서는 비교용 baseline으로 물리 기반 RTT 보정 및 Weighted Robust Least Squares 방식을 함께 정리하였다. 실험 1은 기지국별 RTT bias를 보정하고, 오차 표준편차를 이용해 기지국별 weight를 계산한 뒤, Weighted Robust Least Squares와 residual correction을 적용하는 방식이다. 반면 본 프로젝트의 핵심인 DQN 방식은 거리값으로 좌표를 직접 계산하지 않고, 에이전트가 맵 위에서 이동하면서 최종 위치를 찾아가는 강화학습 기반 순차적 의사결정 방식이다. ChatGPT는 이 두 방식의 차이를 보고서 문장으로 정리하는 데 도움을 주었다.

보고서 작성 단계에서도 ChatGPT를 활용하였다. 실험 과정에서 얻은 결과를 문장으로 정리하고, 알고리즘 설명을 상태, 행동, 보상, 학습 과정, 추론 과정으로 나누어 서술하는 데 도움을 받았다. 또한 위치 오차, Mean Error, RMSE 등의 수식을 GitHub Markdown에서 깨지지 않도록 정리하는 데 활용하였다. 하지만 보고서의 전체 흐름, 강화학습을 메인으로 설명하는 구성, 실험 1을 비교용 baseline으로 두는 방향, 최종 실험 결과의 해석은 본인이 결정하였다.

본 프로젝트에서 Agent AI의 역할과 본인의 역할은 다음과 같이 구분할 수 있다.

| 구분       | Agent AI의 역할                  | 본인의 역할                                          |
| -------- | ----------------------------- | ----------------------------------------------- |
| 방법 선택    | 강화학습의 장단점과 한계 설명              | 강화학습을 메인 알고리즘으로 사용하기로 결정                        |
| 문제 설계    | 강화학습 개념과 DQN 구조 설명            | 위치 추정 문제를 맵 탐색 문제로 재구성                          |
| 상태·행동·보상 | 설계 내용을 강화학습 용어로 정리            | 상태, 행동, 보상 구조를 직접 설계                            |
| 코드 구현    | DQN 코드 구조와 학습 흐름 검토           | `train.py`, `main.py` 작성·수정·실행                  |
| 실험 설계    | 실험 결과 해석과 다음 방향 조언            | 시작 위치, step size, max step, 보상 함수 등 실험 조건 직접 설계 |
| 결과 판단    | 성능 변화 원인 해석 보조                | validation 결과를 비교하여 최종 설정 판단                    |
| 보고서 작성   | 문장 다듬기, 수식 표현, Markdown 형식 정리 | 보고서 전체 흐름 결정, 실험 결과 반영, 최종 내용 검토                |

따라서 본 프로젝트에서 ChatGPT는 강화학습 알고리즘을 대신 설계하거나 실험을 대신 수행한 도구가 아니라, 강화학습 개념 이해와 코드 구조 검토, 결과 해석, 보고서 정리를 보조한 Agent AI로 활용되었다. 강화학습을 선택한 것, 문제를 맵 탐색 문제로 변환한 것, 상태·행동·보상을 설계한 것, 실험 조건을 정하고 validation 결과를 비교한 것은 본인이 직접 수행하였다.


## 4. 결과 도출 & 디스커션

### 4.1 비교용 실험 1: 물리 기반 baseline

비교용 실험 1은 RTT 거리값을 먼저 기지국별로 보정한 뒤, Weighted Robust Least Squares로 1차 위치를 추정하고, Ridge Regression으로 residual correction을 수행하는 방식이다. 이 방법은 강화학습과 달리 위치 추정 문제의 기하학적 구조를 직접 사용한다.

물리 기반 baseline에서는 기지국별 평균 오차를 bias로 계산하여 거리값에서 제거하고, 오차 표준편차가 작은 기지국에 더 큰 weight를 부여하였다. 기지국 $i$의 bias와 weight는 개념적으로 다음과 같이 표현할 수 있다.

$$
b_i = \frac{1}{N}\sum_{u=1}^{N}(\hat{d}_{i,u}-d^{true}_{i,u})
$$

$$
w_i = \frac{1}{\sigma_i+\epsilon}
$$

이후 Huber loss를 적용한 robust least squares로 1차 위치를 구하고, 보정된 거리값, 거리값 제곱, WLS 위치, 각 기지국 residual을 feature로 사용하여 Ridge Regression이 잔여 오차를 보정하였다.

최종 비교 기준으로는 validation에서 가장 안정적이었던 ridge alpha 20.0, Huber scale 2.0 결과를 사용하였다.

| 방법 | Mean | Median | RMSE | 90% Error | Max |
|---|---:|---:|---:|---:|---:|
| 물리 기반 baseline | 8.1541 | 7.4217 | 9.3769 | 14.5336 | 25.9557 |

이 결과는 평균 오차와 RMSE 측면에서 DQN보다 좋다. 그 이유는 물리 기반 방법이 기지국 위치와 거리 방정식이라는 문제의 구조를 직접 사용하기 때문이다. 따라서 이 실험은 강화학습 모델의 우수성을 보이기 위한 대상이라기보다, 강화학습 접근이 현재 어느 정도까지 도달했는지 판단하기 위한 비교 기준으로 사용하였다.

### 4.2 DQN 강화학습 실험 결과

DQN 실험에서는 여러 보상 구조와 학습 설정을 바꾸어가며 validation 성능을 비교하였다. 아래 표는 주요 실험 결과이다.

| 실험 | 변경 내용 | Mean | Median | RMSE | 90% Error | Max |
|---|---|---:|---:|---:|---:|---:|
| RL 기본 상태 | 현재 위치 + 거리 18개, 계단형 STOP 보상 | 11.4421 | 9.9606 | 13.3172 | 19.8977 | 42.7899 |
| Train center 시작 | 학습 시작 위치를 중앙으로 고정 | 12.3564 | 10.7424 | 15.4761 | 21.6726 | 73.0289 |
| 큰 패널티 추가 | MAX_STEPS까지 STOP하지 않으면 큰 패널티 | 16.1153 | 14.2446 | 19.2918 | 26.8842 | 72.0442 |
| Step size 2.0 | 이동 크기 증가, max step 감소 | 13.5513 | 11.0951 | 17.2593 | 25.4187 | 74.4052 |
| 약한 보상 | 성공 보상과 실패 패널티 약화 | 12.5614 | 10.7989 | 15.7043 | 21.9247 | 89.7229 |
| 성공 반경 확대 | 성공/중간 반경만 확대 | 12.9212 | 11.0605 | 15.5549 | 22.3089 | 53.4375 |
| SmoothL1 + clipping | 손실 함수와 gradient 안정화 시도 | 13.0184 | 10.8430 | 17.5517 | 21.8823 | 78.8257 |
| Double DQN | target 계산 방식 변경 | 13.1227 | 11.2633 | 15.6172 | 20.9595 | 53.4257 |
| main random 시작 | validation 시작 위치도 random | 11.8062 | 9.8776 | 14.6802 | 19.8717 | 81.5200 |
| 장기 학습 | episode, memory 증가 및 learning rate 감소 | 13.6849 | 11.1749 | 17.3156 | 25.3803 | 66.7428 |
| Gaussian STOP | 부드러운 STOP 보상 | 12.9778 | 11.6913 | 15.4642 | 24.0179 | 54.6923 |
| Gaussian + 거리 패널티 | STOP 실패 패널티 강화 | 18.4399 | 12.7522 | 27.3031 | 30.7579 | 125.7699 |
| tanh STOP | tanh 기반 STOP 보상 | 13.0444 | 10.7440 | 16.5238 | 22.8258 | 75.1416 |
| BS 위치 추가 | 상태에 기지국 좌표 추가 | 13.0544 | 9.7394 | 19.3397 | 21.4499 | 111.0585 |
| BS 추가 + LR 5e-4 | 기지국 좌표 추가 후 learning rate 감소 | 12.5112 | 11.2900 | 14.2339 | 21.7370 | 37.4462 |
| LR 7e-4 | learning rate 조정 | 12.1920 | 10.5062 | 15.3873 | 20.3395 | 90.9284 |
| Episodes 5000 | 학습 episode 증가 | 13.3989 | 10.5041 | 17.1059 | 25.2406 | 62.6585 |
| Smooth Step | sigmoid 기반 부드러운 STOP 보상 | 14.4162 | 10.6018 | 22.0818 | 22.4522 | 116.1854 |
| Smooth Step 조정 | STOP base, bonus, tau 조정 | 12.3370 | 10.7603 | 15.0141 | 21.3639 | 70.1554 |

전체 결과를 보면 RL 기본 상태 실험이 평균 오차와 RMSE 기준으로 가장 좋았다. 기지국 위치를 상태에 추가하거나, Double DQN을 적용하거나, 부드러운 STOP 보상을 사용하는 시도는 대부분 기대한 만큼의 개선을 만들지 못했다. 특히 부드러운 보상 계열은 max error가 크게 증가하는 경우가 많았다.

### 4.3 최종 선택한 강화학습 설정

최종 DQN 설정은 기본 상태와 계단형 STOP 보상을 사용한 실험이다. 이 설정은 mean error 11.4421m, RMSE 13.3172m, 90% error 19.8977m를 기록하였다.

| 최종 RL 설정 | 값 |
|---|---|
| 알고리즘 | DQN |
| 상태 | 현재 위치 2개 + 거리값 18개 |
| 행동 | 위, 아래, 왼쪽, 오른쪽, 멈춤 |
| 시작 위치 | 학습 시 random, 평가 시 center |
| 이동 보상 | 거리 오차 감소량 기반 |
| STOP 보상 | 2m 이내 +20, 5m 이내 +5, 그 외 -20 |
| 최종 validation mean error | 11.4421m |
| 최종 validation RMSE | 13.3172m |

이 설정을 선택한 이유는 단순히 첫 번째 실험이기 때문이 아니다. 이후 여러 실험에서 보상, 상태, 학습량을 수정했지만 평균 오차와 RMSE가 대부분 악화되었다. 따라서 현재 데이터와 DQN 구조에서는 복잡한 보상보다 명확한 계단형 STOP 보상과 기본 상태 구성이 가장 안정적이었다.

### 4.4 물리 기반 baseline과 DQN 비교

| 방법 | Mean | Median | RMSE | 90% Error | Max |
|---|---:|---:|---:|---:|---:|
| 물리 기반 baseline | 8.1541 | 7.4217 | 9.3769 | 14.5336 | 25.9557 |
| DQN 강화학습 | 11.4421 | 9.9606 | 13.3172 | 19.8977 | 42.7899 |

수치상으로는 물리 기반 baseline이 DQN보다 더 좋은 성능을 보였다. 평균 오차는 약 3.29m, RMSE는 약 3.94m 차이가 났다. 특히 최대 오차도 물리 기반 baseline이 더 낮았다.

하지만 이 비교는 완전히 같은 조건의 알고리즘 비교라고 보기는 어렵다. 물리 기반 baseline은 기지국 위치와 거리 방정식을 직접 사용하고, 기지국별 bias와 weight를 계산하며, Ridge Regression으로 잔여 오차까지 보정한다. 즉 문제의 물리적 구조를 강하게 활용한다. 반면 DQN은 현재 위치와 거리값을 상태로 받아 이동 정책을 학습하는 방식이다. DQN은 직접적인 삼변측량 공식을 사용하지 않고, 이동과 멈춤 행동을 통해 위치를 찾는다.

따라서 본 실험에서 얻은 결론은 “DQN이 물리 기반 방법보다 우수하다”가 아니라, “RTT 위치 추정 문제를 강화학습 문제로 구성할 수는 있지만, 현재 설계에서는 물리 기반 baseline보다 성능이 낮다”이다. 특히 위치 추정처럼 기하학적 구조가 강한 문제에서는 그 구조를 직접 활용하는 방법이 여전히 강력하다.

### 4.5 실패한 실험에서 얻은 해석

첫째, 보상을 약하게 만들면 정책이 애매해졌다. 실험 5에서는 성공 보상과 실패 패널티를 모두 줄였는데, mean error와 max error가 악화되었다. 이는 정답 근처에서 멈추는 행동의 이득과 멀리서 멈추는 행동의 손해가 모두 약해져서 STOP 판단이 흐려졌기 때문으로 보인다.

둘째, 부드러운 STOP 보상은 기대와 다르게 성능을 악화시켰다. Gaussian, tanh, sigmoid 기반 보상은 이론적으로는 더 자연스러운 보상처럼 보이지만, 실제 DQN 학습에서는 계단형 보상보다 명확한 성공/실패 신호를 주지 못했다. 특히 max error가 100m 이상으로 커진 실험도 있었기 때문에, 일부 샘플에서 정책이 완전히 잘못된 행동을 강하게 선택한 것으로 볼 수 있다.

셋째, 상태에 기지국 좌표를 추가하는 것이 항상 좋은 것은 아니었다. 기지국 위치를 추가하면 정보량은 늘어나지만, 상태 차원이 커지고 학습해야 할 관계도 복잡해진다. 실험 14에서는 median은 약간 좋아졌지만 RMSE와 max error가 크게 나빠졌다. learning rate를 낮춘 실험 15에서는 max error가 개선되었지만 mean과 RMSE는 기본 DQN보다 나빴다.

넷째, 학습량을 늘리는 것만으로는 성능이 좋아지지 않았다. episode를 크게 늘리거나 replay memory를 증가시킨 실험에서도 성능이 개선되지 않았다. 이는 현재 문제에서 단순히 더 오래 학습하는 것보다, 상태 표현과 보상 구조 자체가 더 중요하다는 것을 보여준다.

### 4.6 한계 및 future work

본 실험의 가장 큰 한계는 DQN이 위치 추정 문제의 기하학적 구조를 충분히 활용하지 못했다는 점이다. 현재 상태는 현재 위치와 거리값으로 구성되어 있지만, 각 기지국과 현재 위치 사이의 예상 거리, 측정 거리와 예상 거리의 residual, 기지국별 신뢰도 같은 정보는 직접적으로 포함하지 않았다. 이러한 정보는 물리 기반 baseline에서 성능 향상에 중요한 역할을 했기 때문에, 강화학습에서도 상태에 더 구조적으로 포함할 필요가 있다.

또한 현재 행동은 1m 단위의 상하좌우 이동으로 제한되어 있다. 이 방식은 구현이 단순하지만, 실제 위치 추정 문제에서는 연속 좌표를 바로 예측하는 actor-critic 계열 방법이 더 적합할 수 있다. 예를 들어 DDPG, TD3, SAC 같은 연속 제어 알고리즘을 사용하면 (delta x, delta y) 또는 최종 위치 보정량을 연속값으로 출력할 수 있다.

마지막으로 현재 DQN은 각 샘플을 독립적인 환경으로 보고 학습한다. 그러나 RTT 오차의 분포나 기지국별 신뢰도는 전체 데이터에서 반복적으로 나타나는 구조이므로, 강화학습과 지도학습을 결합한 hybrid 방식도 고려할 수 있다. 예를 들어 물리 기반 WLS 결과를 초기 위치로 사용하고, DQN은 그 주변에서 보정 행동만 수행하게 만들면 탐색 공간이 줄어들어 성능이 개선될 가능성이 있다.

## 5. Reference

| 참고 자료 | 보고서에서 사용한 부분 | 본 실험과의 차이 |
|---|---|---|
| Mnih et al., Human-level control through deep reinforcement learning, Nature, 2015 | DQN의 기본 개념, Q-network와 target network 구조 | 원 논문은 Atari 화면 기반 제어 문제이고, 본 실험은 RTT 거리값 기반 2차원 위치 추정 문제이다. |
| Huber, Robust Estimation of a Location Parameter, 1964 | 비교용 물리 기반 baseline에서 Huber loss 해석 | 본 보고서의 중심 알고리즘은 DQN이며, Huber loss는 비교 실험에서만 사용하였다. |
| Hoerl and Kennard, Ridge Regression, 1970 | Residual Correction에서 Ridge Regression 해석 | Ridge는 물리 기반 baseline의 잔여 오차 보정용이며, 강화학습 모델 자체에는 사용하지 않았다. |
| Sutton and Barto, Reinforcement Learning: An Introduction | 상태, 행동, 보상, 정책의 기본 강화학습 개념 | 교재는 일반 강화학습 이론을 설명하며, 본 실험은 RTT 위치 추정에 맞게 환경과 보상을 직접 설계하였다. |

